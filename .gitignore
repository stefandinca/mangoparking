node_modules
dist
.env
.env.local
.env.*.local
*.log
.DS_Store
Thumbs.db
.firebase
.claude/settings.local.json
src/seed.js

# Local-only credential / secret material — never commit
netopia*keys*.md
netopia*keys*.txt
documentation/smartbill-creds.md
*.pem
*.key
*.cer
secrets/

# i18n analysis scratch dumps — throwaway, not product
en_keys.txt
ro_keys.txt
used_keys.txt
locale_params.json

# Raw client image masters — optimized web copies live in public/images/
src/images/
